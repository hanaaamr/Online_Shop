spring:
  application:
    name: LoginRegAPI
  datasource:
    url: jdbc:mysql://localhost:3306/hanouna
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  logging:
    level:
      org:
        hibernate:
          sql: debug